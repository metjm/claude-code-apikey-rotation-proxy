{
  "name": "claude-code-apikey-rotation-proxy",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "claude-proxy": "src/server.ts",
    "claude-proxy-ctl": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
